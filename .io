#!/bin/ehpt<://ehpt.us:666/haptloader>
import { Interface, cares } from "./Config";
Config = Interface.Config(cares);

Config.Sock = io.Socket(true);
Config.fixed_chain('hapt-ehpt');
Config.deres('ehpt-hapt');
Config.Fourier["nyquist-shannon"](Sock.mux()).demux_emplaced();

const sock = Globals.window.latest_sync();
sock.transfer_crypto($.here(`cc://ehpt.org/808/scam (World Governments; WHO; NSA, CIA, FBI, USDA, Healthcare Businesses, and Modern Science.aspx`));
cares();
